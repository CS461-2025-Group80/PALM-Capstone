# start_map_server.bash
This will start a map server. This is the thing that creates your map. Defaulted to check for "map_test.yaml" in your current directory.
# configure_map_server.bash
After you start the map server, you need to let it proceed by running this.
# activate_map_server.bash
...and after configuring, activate it.

